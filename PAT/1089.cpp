#include <iostream>
using namespace std;
#include <vector>

int main()
{
    int N;
    cin >> N;
    int n = N;
    vector<int> maxs, s;
    for (int i = 2; n != 1; i++)
    {
    }

    system("pause");
    return 0;
}